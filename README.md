# Автоматизация откликов на hh.ru

Скрипт для автоматизации поиска вакансий и отправки откликов на сайте [hh.ru](https://hh.ru/) с поддержкой заполнения обязательных сопроводительных писем.

## Возможности

- ✅ Автоматический вход через телефон и SMS
- ✅ Поиск вакансий по настраиваемым запросам
- ✅ Извлечение текста вакансий
- ✅ Автоматическая отправка откликов
- ✅ Заполнение обязательных сопроводительных писем
- ✅ Пропуск вакансий с тестами/вопросами
- ✅ Скрытие обработанных вакансий

## Требования

- Python 3.8+
- Playwright

## Установка

1. Клонируйте репозиторий или скачайте файлы

2. Установите зависимости:

```bash
pip install playwright
```

3. Установите браузеры для Playwright:

```bash
playwright install chromium
```

## Использование

### Базовое использование

Запустите скрипт без параметров - он запросит все необходимые данные:

```bash
python main.py
```

### Использование с параметрами

```bash
python main.py \
  --phone "+79991234567" \
  --search "React Next.js разработчик" \
  --limit 20 \
  --extract-texts
```

### Параметры командной строки

- `--phone` - Номер телефона для входа (например, `+79991234567`)
- `--sms-code` - Код из SMS (если не указан, будет запрошен интерактивно)
- `--search` - Поисковый запрос (например, `"React Next.js разработчик"`)
- `--search-role` - Использовать готовый запрос для роли (`react_nextjs`, `qa_lead`, `backend`)
- `--cover-letter` - Путь к файлу с шаблоном сопроводительного письма или сам текст
- `--extract-texts` - Извлекать тексты вакансий (замедляет работу, но позволяет анализировать вакансии)
- `--limit` - Максимальное количество вакансий для отклика (по умолчанию: 10)

### Примеры использования

#### Поиск React/Next.js разработчика

Используя готовый запрос из `search_queries.py`:

```bash
python main.py \
  --phone "+79991234567" \
  --search-role react_nextjs \
  --limit 15
```

Или с кастомным запросом:

```bash
python main.py \
  --phone "+79991234567" \
  --search '(React OR "React.js" OR Next.js OR NextJS) AND (TypeScript OR JavaScript) NOT (стажер OR intern OR junior OR "без опыта")' \
  --limit 15
```

#### С кастомным сопроводительным письмом

Создайте файл `cover_letter.txt`:

```
Здравствуйте!

Меня заинтересовала вакансия. Имею опыт работы с React и Next.js более 3 лет.
Готов обсудить детали и ответить на ваши вопросы.

С уважением,
[Ваше имя]
```

Запустите:

```bash
python main.py \
  --phone "+79991234567" \
  --search "React Next.js разработчик" \
  --cover-letter cover_letter.txt
```

#### С извлечением текстов вакансий

```bash
python main.py \
  --phone "+79991234567" \
  --search "React Next.js разработчик" \
  --extract-texts \
  --limit 5
```

## Как составлять поисковые запросы на hh.ru

Главная идея: запрос должен быть достаточно широким, чтобы давать поток вакансий, и достаточно точным, чтобы не собирать мусор.

### Структура запроса

```
(ROLE) AND (STACK) AND (DOMAIN) NOT (BLACKLIST)
```

Где:
- **ROLE** - названия роли/должности
- **STACK** - технологии и инструменты (якоря)
- **DOMAIN** - область применения (опционально)
- **BLACKLIST** - исключения (стажеры, джуны и т.д.)

### Примеры для Frontend разработчика React/Next.js

#### 1. Сначала делаем словарь названий роли:

```
("Frontend разработчик" OR "Frontend Developer" OR "React разработчик" OR "React Developer" OR "Next.js разработчик")
```

#### 2. Потом добавляем якоря (технологии):

```
(React OR "React.js" OR Next.js OR NextJS OR TypeScript OR JavaScript)
```

#### 3. Потом уточняем:

```
(React OR Next.js) AND (TypeScript OR JavaScript) AND (Redux OR Zustand) NOT (стажер OR intern OR junior)
```

### Готовые запросы

В файле `search_queries.py` есть готовые примеры поисковых запросов:

```python
from search_queries import get_default_query, get_all_queries

# Получить запрос по умолчанию для React/Next.js
query = get_default_query("react_nextjs")

# Получить все запросы для роли
queries = get_all_queries("react_nextjs")
```

### Примеры запросов

#### Базовый запрос React/Next.js:

```
(React OR "React.js" OR Next.js OR NextJS) AND (TypeScript OR JavaScript) NOT (стажер OR intern OR junior OR "без опыта")
```

#### С акцентом на Next.js:

```
("Next.js" OR NextJS OR "Next JS") AND (TypeScript OR JavaScript) AND (SSR OR "Server Side Rendering") NOT (стажер OR intern)
```

#### С полным стеком:

```
(React OR Next.js) AND (TypeScript OR JavaScript) AND (Redux OR Zustand) AND (Tailwind OR "styled-components") NOT (стажер OR intern OR junior)
```

## Логин через телефон с SMS

### Процесс входа

1. Скрипт открывает страницу hh.ru
2. Нажимает кнопку "Войти"
3. Вводит номер телефона
4. Нажимает "Дальше"
5. Ожидает код из SMS (если не указан в параметрах)
6. Вводит код и завершает вход

### Пример

```bash
# Интерактивный ввод (код будет запрошен)
python main.py --phone "+79991234567"

# С предустановленным кодом
python main.py --phone "+79991234567" --sms-code "12345"
```

**Примечание:** Код из SMS обычно действителен ограниченное время, поэтому лучше использовать интерактивный ввод.

## Сопроводительное письмо

### Автоматическая генерация

Если не указан шаблон, скрипт автоматически генерирует базовое сопроводительное письмо на основе названия вакансии.

### Кастомный шаблон

Создайте файл с шаблоном (например, `cover_letter.txt`):

```
Здравствуйте!

Меня заинтересовала вакансия "{vacancy_title}".

[Ваш текст]

С уважением,
[Ваше имя]
```

**Примечание:** В текущей версии переменная `{vacancy_title}` не подставляется автоматически, но вы можете использовать любой текст.

### Использование шаблона

```bash
python main.py --cover-letter cover_letter.txt
```

Или напрямую текст:

```bash
python main.py --cover-letter "Здравствуйте! Меня заинтересовала вакансия. С уважением."
```

## Как работает скрипт

1. **Вход в систему** - Автоматический вход через телефон и SMS
2. **Поиск вакансий** - Выполнение поискового запроса
3. **Прогрузка результатов** - Автоматический скролл для загрузки всех вакансий
4. **Сбор вакансий** - Фильтрация вакансий с кнопкой "Откликнуться"
5. **Извлечение текстов** (опционально) - Открытие каждой вакансии и извлечение описания
6. **Отправка откликов**:
   - Клик по кнопке "Откликнуться"
   - Если требуется сопроводительное письмо - заполнение и отправка
   - Если требуется тест/вопросы - пропуск и скрытие вакансии
   - Если отклик отправлен - продолжение

## Обработка различных сценариев

### ✅ Успешный отклик

Скрипт отправляет отклик и продолжает работу.

### ✍️ Обязательное сопроводительное письмо

Если указан шаблон письма, скрипт автоматически заполняет и отправляет его. Если шаблона нет - вакансия пропускается и скрывается.

### 🧠 Тест/вопросы работодателя

Вакансии с тестами или вопросами пропускаются и скрываются.

### ℹ️ Дополнительные шаги

Вакансии, требующие дополнительных действий, пропускаются и скрываются.

## Ограничения

- Скрипт работает только с вакансиями, у которых есть кнопка "Откликнуться" в карточке
- Вакансии с тестами/вопросами пропускаются
- Скрипт не может обойти капчу (если появится)
- Рекомендуется использовать с разумными лимитами (не более 20-30 откликов за раз)

## Безопасность

- Номер телефона и код SMS не сохраняются
- Все данные обрабатываются локально
- Рекомендуется не передавать чувствительные данные через параметры командной строки (они могут быть видны в истории)

## Устранение неполадок

### Ошибка "Поле ввода телефона не найдено"

- Убедитесь, что вы используете последнюю версию скрипта
- Проверьте, что сайт hh.ru доступен
- Возможно, изменилась структура страницы - сообщите об этом

### Ошибка "Кнопка отправки не найдена"

- Модалка отклика может иметь другую структуру
- Попробуйте обновить скрипт или сообщите об ошибке

### Скрипт не находит вакансии

- Проверьте правильность поискового запроса
- Убедитесь, что вы залогинены
- Попробуйте упростить запрос

## Лицензия

Этот скрипт предоставляется "как есть" для личного использования. Используйте ответственно и в соответствии с правилами использования hh.ru.

## Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что используете актуальную версию скрипта
3. Проверьте логи выполнения скрипта

---

**Важно:** Используйте скрипт ответственно. Чрезмерное количество автоматических откликов может привести к ограничениям со стороны hh.ru.


## ☕ Поддержать проект

Проект развивается в свободное время.  
Если он оказался полезным — можно поддержать на кофе:

💳 СБП / Карта РФ:  
`+7 (901) 301-13-97`
🌐 Boosty: https://boosty.to/frontender89/donate



